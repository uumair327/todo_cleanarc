/// Severity level of an architectural violation
enum Severity {
  /// Critical violations that severely impact architecture
  critical,
  
  /// Major violations that significantly impact maintainability
  major,
  
  /// Minor violations that should be addressed but are not urgent
  minor,
}
